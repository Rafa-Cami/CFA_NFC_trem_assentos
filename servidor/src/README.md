# Para implementar o projeto: 

Nessa pasta encontra-se o código para rodar o servidor necessário para fazer a comunicação entre os ESP32

Antes de ligar o ESP32 NFC, rode o servidor:

```powershell
python pc_server.py
```
ou (se estiver na pasta principal do repositório):

```powershell
python servidor\src\pc_server.py
```
